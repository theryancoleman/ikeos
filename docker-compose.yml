services:
  ikeos:
    build: .
    container_name: ikeos
    restart: unless-stopped
    env_file: .env
    ports:
      - "5009:5009"
    volumes:
      - ${VAULT_PATH}:/vault:rw
      - ${CLAUDE_VERSION_PATH}:/claude-config/VERSION:ro
